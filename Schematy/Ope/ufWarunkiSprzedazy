

USE Stacja_Paliw
GO


 IF OBJECT_ID('Ope.ufWarunkiSprzedazy') IS NOT NULL
 DROP FUNCTION Ope.ufWarunkiSprzedazy;
 GO

--Instukcja sprawdzajaca czy istnieje juz procedura Ope.WarunkiSprzedazy jesli tak to jest likwidowana. Zakladamy, ze jezeli istniala to przez pomylke--

CREATE FUNCTION Ope.WarunkiSprzedazy(@ID int)

RETURNS varchar (7)
WITH SCHEMABINDING
AS
BEGIN
DECLARE @War varchar(7)=(SELECT WarunkiPlatnosci FROM Ope.Klienci WHERE IdKlienta=@ID)
RETURN @War;
END;

/*funkcja zwracajaca sposob platnosci danego klienta po podaniu jego id*/

